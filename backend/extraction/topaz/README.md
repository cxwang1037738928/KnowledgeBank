# Topaz — general document crawler (planned)

Will parse arbitrary documents (reports, manuals, articles) that have no
academic or medical structure. Not yet implemented.

Reuses the crawler-agnostic stages in `extraction/` (chunker.js, embed.js,
generate_categories.js, regex_utils.js); general-purpose extraction and
ranking land here.
